import 'package:flutter/material.dart';

class ToolPalette extends StatelessWidget {
  const ToolPalette({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 72, child: Placeholder());
  }
}
